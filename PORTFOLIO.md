# 프로젝트 소개 (포트폴리오용)

## 한 줄 요약

JLPT 학습자를 위한 개인 단어장 웹 서비스. 단어 카드 학습, 라이트너 박스 기반 복습, 딕테이션(듣고 받아쓰기)·타자연습, JWT 인증(회전 방식), VOICEVOX 기반 고품질 TTS까지 포함한 풀스택 프로젝트. 기획·설계·백엔드·프론트엔드·배포를 전부 직접 진행.

## 기술 스택

- **백엔드**: Spring Boot 3.3, Java 21, Spring Data JPA, Spring Security 6, MySQL
- **인증**: JWT 액세스 토큰(30분) + DB 저장 리프레시 토큰(14일, 회전 방식)
- **프론트엔드**: React 19, TypeScript, Vite, react-router-dom v7
- **테스트**: JUnit5 + Mockito(백엔드), Vitest + React Testing Library(프론트엔드)
- **음성**: VOICEVOX(엔진 프록시) + Web Speech API(폴백)
- **배포**: Railway(백엔드/MySQL/VOICEVOX), Vercel(프론트엔드), Docker 멀티스테이지 빌드

## 핵심 기능

1. **단어 카드 학습**: JLPT 레벨(N5~N1)별 단어 랜덤 조회, 예문 내 단어 클릭 시 해당 단어로 이동
2. **내 단어장 + 복습**: 라이트너 박스(5단계) 스페이스드 리피티션 알고리즘으로 복습 스케줄 자동 관리
3. **딕테이션**: 문장을 듣고 로마자로 받아쓰기 → 실시간 히라가나 변환(wanakana) → 후리가나 기준 관대한 채점(레벤슈타인 유사도)
4. **타자연습**: 목표 문장을 보면서 빠르고 정확하게 타이핑, CPM(분당 글자 수) 측정
5. **인증**: 회원가입(추천인/목표레벨/유입경로 등 확장 필드), 이메일 인증·아이디 찾기·비밀번호 재설정(mock 메일), 토큰 회전 기반 세션 관리

## 아키텍처 설계 포인트

- **선택적 인증(Optional Auth) 패턴**: 단어 조회·딕테이션처럼 "로그인 없이도 쓸 수 있지만 로그인하면 더 잘 동작하는" 엔드포인트를 `SecurityUtils.getCurrentUserId(): Optional<Long>`로 하나의 컨트롤러 코드에서 처리. 인증 필수 API는 `requireCurrentUserId()`로 구분.
- **토큰 회전(Refresh Token Rotation)**: 리프레시할 때마다 기존 토큰을 폐기하고 새로 발급 — 탈취된 토큰이 재사용되면 즉시 무효화되는 구조.
- **딕테이션 정답 비노출 설계**: `/dictation/random`은 정답(읽기)을 응답에 아예 포함하지 않는다. 개발자 도구 네트워크 탭만 봐도 정답이 보이는 실수를 설계 단계에서 차단. 반면 타자연습(`/dictation/practice-random`)은 같은 문장 풀을 쓰지만 목적이 다르므로 정답을 처음부터 보여주는 별도 엔드포인트로 분리.
- **TTS 이중화**: 백엔드가 VOICEVOX를 프록시하되, 연결 실패 시 프런트가 자동으로 브라우저 기본 음성으로 전환. 단일 장애점을 만들지 않는 폴백 설계. 동일 (문장, 속도) 조합은 클라이언트에서 캐싱하고, 문장이 로드되는 즉시 백그라운드로 프리로드해서 체감 속도를 개선.

자세한 설계는 [`ARCHITECTURE.md`](./ARCHITECTURE.md)(ERD, API 명세, 인증 흐름), 테스트 설계는 [`TEST_CASES.md`](./TEST_CASES.md) 참고.

## 트러블슈팅 사례

배포/개발 과정에서 실제로 겪고 해결한 문제들. 단순 나열이 아니라 "어떻게 진단했는지"에 초점.

### 1. Railway 배포 직후 서버가 계속 crash

**증상**: Railway에 처음 배포했을 때 서버가 기동되자마자 죽는 걸 반복.
**진단**: 크래시 로그를 확인하니 `JwtTokenProvider` 빈 생성 중 `Keys.hmacShaKeyFor()`에서 예외 발생. HS256 알고리즘은 최소 256비트(32바이트) 키를 요구하는데, 설정된 `JWT_SECRET`이 25자에 불과했던 것.
**해결**: `openssl rand -base64 32`로 32바이트 랜덤 시크릿을 생성해서 환경변수로 교체. 이후 로컬 개발용 기본값도 32자 이상으로 맞춰서 같은 문제가 재발하지 않게 함.
**배운 점**: 프레임워크 예외 메시지를 끝까지 읽고, "왜 이 예외가 나는가"를 라이브러리 요구사항(HS256 최소 키 길이) 수준까지 추적하는 습관.

### 2. Vercel 배포 후 CORS 에러

**증상**: 프론트는 정상 배포됐는데 API 호출 시 `No 'Access-Control-Allow-Origin' header` 에러.
**진단**: 두 가지를 동시에 의심 — ① Railway가 새 `CORS_ALLOWED_ORIGINS` 값으로 아직 재배포되지 않았을 가능성, ② 콤마로 구분한 오리진 목록에 공백이 들어가서 두 번째 값이 제대로 매칭 안 될 가능성(`origin1, origin2`처럼 공백이 있으면 `origin2` 앞에 공백이 붙어 정확히 일치하지 않음).
**해결**: 재배포 타임스탬프 확인 + 공백 제거. `setAllowedOriginPatterns`(패턴 매칭 지원)를 써서 `https://*.vercel.app`처럼 와일드카드로 프리뷰 배포 도메인까지 한 번에 허용하도록 개선.

### 3. Railway 프라이빗 네트워킹에서 서비스 간 통신 DNS 실패

**증상**: 백엔드에서 VOICEVOX 엔진 서비스로 연결이 안 됨 — `curl: Could not resolve host`.
**진단**: 서비스 이름에 언더스코어(`voicevox_engine`)가 들어간 게 원인일 거라 추정하고 이름을 바꿔봤지만 여전히 실패. 결국 추측 대신 Railway의 **Settings → Networking**에서 실제로 부여된 프라이빗 도메인을 직접 확인하는 방식으로 전환.
**해결**: 실제 도메인(`voicevoxengine.railway.internal`, 언더스코어가 제거된 형태)을 확인해서 정확히 반영. 이 경험으로 "그럴듯한 추측"보다 "플랫폼이 실제로 뭘 주는지 직접 확인"하는 디버깅 순서를 재확인.

### 4. 사용자가 정확히 타이핑했는데도 "정확도 0%, 인식 못함"으로 나옴

**증상**: 딕테이션 페이지에서 로마자를 입력하면 화면에는 히라가나로 잘 바뀌어 보이는데, 제출하면 항상 "입력 없음"으로 채점됨.
**진단 과정**: 처음엔 사용자의 OS 일본어 IME가 켜져 있어서 충돌하는 문제로 추정했으나(엔터를 눌러야 한자 변환되는 증상과 맞아떨어져 보였음), 사용자가 "로마자 그대로 입력한 게 맞다"고 재확인하면서 가설을 뒤집고 코드를 재검토. 원인은 `useRomajiInput` 훅의 `useEffect(() => {...}, [])`가 컴포넌트 최초 렌더링 시점에 실행되는데, 이 시점엔 `<input>`이 아직 DOM에 없었던 것(문장을 비동기로 불러온 뒤에야 조건부로 렌더링되는 구조라서). 리스너가 실제 input에는 한 번도 안 붙은 채였다.
**해결**: `useRef` + `useEffect` 조합 대신 **콜백 ref**로 전환 — React가 DOM 노드를 실제로 붙이거나 뗄 때마다 직접 호출해주므로 조건부 렌더링과 무관하게 항상 정확한 타이밍에 리스너가 붙는다.
**검증**: 이 버그를 그대로 재현하는 Vitest 테스트(`useRomajiInput.test.tsx`)를 작성해서, 지연 마운트 시나리오에서도 정상 동작함을 자동화 테스트로 고정.
**배운 점**: 사용자가 준 첫 가설(IME 충돌)에 매몰되지 않고, 사용자의 재확인 한마디에 가설을 즉시 재검토한 것이 실제 원인(React 렌더링 타이밍 버그)을 찾는 데 결정적이었다.

### 5. 대량 문장 데이터의 읽기(후리가나) 정확도 검증

**상황**: 딕테이션 문장을 150개 이상 추가하면서, 후리가나를 전부 손으로 옮겨 적으면 오탈자 위험이 커지는 문제.
**접근**: MeCab 기반 형태소 분석기(fugashi + unidic)로 읽기를 자동 생성하는 파이프라인을 구축. 다만 자동 생성 결과를 맹신하지 않고 전수 검토한 결과, 실제로 14곳에서 이의(異義) 한자를 잘못 읽은 사례를 발견(예: 「今日」를 인사말 「こんにちは」로 오인식, 「彼」를 고어체 「かの」로 오독, 「日本語」를 「にっぽんご」로 읽음, 「何」의 문맥별 읽기 오류 등). 이런 사례를 전부 수동 교정 테이블로 덮어써서 최종 데이터에 반영.
**배운 점**: 자동화 도구는 "손으로 하는 것보다 빠르게, 틀린 부분을 찾기 쉽게" 만들어주는 것이지, 검증을 생략해도 되는 것은 아니라는 걸 실제 수치(14/150 오류)로 확인한 경험.

### 6. 로마자 입력의 잘 알려지지 않은 함정 — apostrophe 규칙

테스트를 작성하던 중 `"konnichiwa"`를 그대로 변환하면 「こんいちわ」로 잘못 변환되는 현상을 발견했다. 원인은 로마자 입력 표준에서 「ん」 뒤에 모음이 올 때는 `kon'nichiwa`처럼 apostrophe로 명시적으로 끊어줘야 한다는 규칙 때문 — 버그가 아니라 로마자 입력 자체의 특성이었다. 이 케이스를 테스트로 문서화해서 회귀 방지 + 추후 UI 안내 문구 개선의 근거로 남겼다.

## 테스트 전략

- **백엔드**: 도메인 로직(라이트너 박스 계산, 딕테이션 채점 유사도 알고리즘)은 순수 단위 테스트로, 서비스 레이어는 Mockito로 리포지토리를 격리해서 예외 분기(중복 저장, 타인 리소스 접근 등)를 검증. 컨트롤러는 `@WebMvcTest` + `addFilters=false`로 보안 필터 체인과 분리해서 HTTP 응답 형태만 안정적으로 검증.
- **프론트엔드**: 실제로 발생했던 버그(위 트러블슈팅 4번)를 그대로 재현하는 테스트를 작성해 회귀를 방지. 순수 로직을 가진 컴포넌트(정답 비교, 타이핑 정오답 표시)는 별도 렌더 테스트로 분리해서 빠르고 안정적으로 검증.
- 테스트케이스 설계 문서는 [`TEST_CASES.md`](./TEST_CASES.md) 참고 (도메인별 정상/예외/경계 케이스 표).

## 향후 개선 아이디어

- JLPT 전체 급수 어휘(1만 단어 이상)로 확장
- 실제 SMTP 연동으로 이메일 인증/비밀번호 재설정을 mock에서 실서비스로 전환
- 딕테이션 정답 비교를 문자 위치 기반이 아닌 정식 정렬(edit script) 기반 diff로 고도화
- E2E 테스트(Playwright) 추가
